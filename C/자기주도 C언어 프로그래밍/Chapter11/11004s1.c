//Unstable
#include <stdio.h>

int Gvalue(int x, int y, int z) {
    int temp;
    for (int i = 0; i < 2; i++) {
        if (x > y) {
        }
    }
}

int main() {
    Gvalue(5, 3, 2);
    return 0;
}
