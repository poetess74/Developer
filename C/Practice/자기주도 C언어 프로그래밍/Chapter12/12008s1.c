//Unstable
#include <stdio.h>
#include <math.h>
//#define multiA(x, y) pow((x - y), 2)
//#define multiB(x, y) pow((x + y), 3)

int main() {
    int inputA, inputB;
    scanf("%d %d", &inputA, &inputB);

//    printf("(%d - %d) ^ 2 = %d\n", inputA, inputB, multiA(inputA, inputB));
//    printf("(%d - %d) ^ 3 = %d\n", inputA, inputB, multiB(inputA, inputB));
    return 0;
}
